apiVersion: v1
kind: Service
metadata:
  name: oms-service
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: NodePort
  selector:
    app: oms
  ports:
    - name: http
      port: 80
      targetPort: 3004
